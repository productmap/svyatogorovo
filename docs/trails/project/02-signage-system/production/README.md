# Production-ready SVG (для Illustrator)

В отличие от `mockups/signs/*.svg` (визуальные эскизы для согласования и
презентации), файлы в этой папке готовы к импорту в Adobe Illustrator и
дальше — в производственные процессы (резка плёнки, печать, фрезеровка
ACP-панелей).

## Что делает файл «production-ready»

| Свойство | Mockup | Production |
|---|---|---|
| Корневые `width`/`height` | без единиц (абстракт.) | `Xmm × Ymm` (физический размер) |
| `viewBox` | произвольный | совпадает с физическими размерами |
| Координаты | абстрактные | миллиметры (1 unit = 1 mm) |
| Размер артборда после импорта в AI | "по viewBox" в default units | точные мм |
| Цвет | hex `#714A07` для просмотра | hex + указание Pantone/CMYK/RAL для печати |
| Шрифты | font-family fallback | font-family с инструкцией «Create Outlines» перед сдачей |
| QR | стилизованная заглушка | заглушка с пометкой «заменить перед печатью» |
| Сетка построения | нет | скрытый слой M = 25 мм для проверки модульности |
| Слои/группы | имена для отладки | имена для слоя в AI (логично организованы) |

## Чек-лист после открытия в Illustrator

1. **Проверить артборд.** Должен быть точно нужного размера в мм
   (для T2 — 600 × 1200 мм). Если другой — что-то пошло не так с импортом.
2. **Проверить шрифт.** При отсутствии Myriad Pro Condensed AI покажет
   диалог замены. Установить Myriad Pro из Creative Cloud (входит в подписку
   по умолчанию).
3. **Type → Create Outlines** на все надписи перед сдачей в типографию.
   После этого SVG/AI-файл будет рендериться идентично где бы его ни открыли.
4. **Заменить QR-код.** Сгенерировать настоящий QR (например, на
   `https://святогорово.рф/#sergiy` для T2). Слой `#qr-placeholder`
   удалить или скрыть, вставить новый QR в те же координаты.
5. **Проверить цвет.** Слой Sign body должен быть Pantone 731 C
   (для офсетной печати) либо CMYK 34/65/100/43 (для цифровой).
6. **При желании раскрыть `#construction-grid`** — увидите модульную
   сетку M = 25 мм, на которой построен знак.

## Стандарты-источники

Все production-файлы соответствуют:

- **ГОСТ Р 57581-2017** «Туристские услуги. Информационные знаки системы
  навигации в сфере туризма. Общие требования.»
- **Методическое пособие** Минкультуры РФ (2013, согласовано с Минкомсвязью,
  Минрегионом, Минтрансом) — раздел 3.2 (цвет), 3.3 (шрифт), 3.4 (построение),
  3.5 (пиктограммы), 3.6 (построение знаков-указателей).
- **ГОСТ Р 52290-2004** «Знаки дорожные» — в части модульной сетки M = 25 мм.

## Текущий состав

| Файл | Размер | Тип |
|---|---|---|
| `T2-stela.svg` | 600 × 1200 мм | Стела маршрута (старт/финиш Святогоровского отрезка) |

Остальные типы (T1, T3, T4 ×3, T5, T6) — по запросу. Пиктограммы для них
уже есть в `pictograms/official/vector/silhouette/`, нужно только обернуть
их в production-обвязку по образцу T2.

## Технология построения

Production-SVG строится из mockup'а добавлением:
1. `width="Xmm" height="Ymm"` к корневому `<svg>`.
2. Совпадающего `viewBox="0 0 X Y"`.
3. Документирующих комментариев в шапке и в группах.
4. Скрытого слоя `#construction-grid` с модульной сеткой.
5. Чек-листа в комментарии шапки.

Для T2 это сделано вручную как worked example. При желании можно автоматизировать
скриптом `scripts/build-production-signs.py` (TBD): берёт mockup из `mockups/signs/`,
парсит viewBox, оборачивает в production-формат, добавляет grid-слой.
